// Source entrypoint for the DynamicPageKit Studio VS Code extension.
// The generated CommonJS runtime used by local development lives in ../out/extension.js.
// Run `npm install && npm run compile` in this directory to regenerate it when TypeScript tooling is available.
export {};
